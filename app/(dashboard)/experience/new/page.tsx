import ExperienceForm from "@/app/components/ExperienceForm";

type Props = {};
const Page = (props: Props) => {
    return (
        <div>
            <ExperienceForm/>
        </div>
    );
};
export default Page;
